# StandardRB configuration
# https://github.com/standardrb/standard
#
# ruby_version is intentionally unset — standardrb auto-detects from the
# runtime / .ruby-version, matching the convention used in our other projects.

ignore:
  - "vendor/**/*"
  - "scratchpads/**/*"